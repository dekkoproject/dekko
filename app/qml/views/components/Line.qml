import QtQuick 2.4
import "../../constants"

Rectangle {
    color: Style.lineColor
    implicitWidth: Style.lineSize
    implicitHeight: Style.lineSize
    height: implicitHeight
    width: implicitWidth
}

